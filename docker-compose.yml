version: '3.8'

services: 
  postgres:
    container_name: api_curso-pg
    image: postgres
    ports: 
      - 5432:5432
    environment: 
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=cursos   


